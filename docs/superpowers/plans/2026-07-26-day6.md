# Day 6 实施计划

1. 为词语拼音、字卡库交互、Day 6 清理补充失败测试。
2. 扩展 `dict.js`，让词语按字读取构建期生成的静态拼音表。
3. 重构字卡库工具栏、多选热区、详情入口，补充编辑和软删除弹层。
4. 优化 `word-sheet` 的说明、输入可见性和词语空态。
5. 删除 `quickstartFunctions`，扫描示例残留与敏感信息。
6. 跑全量自动测试、JSON 校验、diff 校验，并通过微信开发者工具自动化走主流程。
7. 更新 MVP 开发计划，提交并推送主分支。
